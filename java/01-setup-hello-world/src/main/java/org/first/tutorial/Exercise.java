package org.first.tutorial;

public class Exercise {

    /**
     * Returns a greeting message.
     *
     * <p>
     * <strong>Instruction:</strong>
     * Implement this method to return a string that says "Hello, " followed by the provided name and an exclamation mark.
     * </p>
     *
     * <p>
     * <strong>Example:</strong>
     * If <code>name</code> is "Robot", the method should return "Hello, Robot!"
     * </p>
     *
     * <p>
     * <strong>Explanation:</strong>
     * In Java, you can combine (concatenate) strings using the <code>+</code> operator.
     * <br>
     * <code>String message = "Hello, " + name + "!";</code>
     * </p>
     */
    public String greet(String name) {
        // DELETE THE LINE BELOW AND PUT YOUR CODE THERE
        return "";
    }
}