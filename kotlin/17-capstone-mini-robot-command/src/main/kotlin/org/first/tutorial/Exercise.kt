package org.first.tutorial

class Exercise {

    class CommandRunner {
        private var distanceTraveled = 0.0

        fun execute(commands: List<String>): List<String> {
            // TODO: implement parsing and execution per README.
            return mutableListOf()
        }

        fun getDistanceTraveled(): Double {
            return distanceTraveled
        }
    }
}
