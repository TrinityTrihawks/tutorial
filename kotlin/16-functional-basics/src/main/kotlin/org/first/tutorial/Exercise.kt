package org.first.tutorial

class Exercise {

    fun filterAbove(readings: List<Double>, minValue: Double): List<Double> {
        // TODO: use Kotlin collection functions to keep values greater than minValue.
        return emptyList()
    }

    fun average(readings: List<Double>): Double {
        // TODO: return average or 0 if empty.
        return 0.0
    }

    fun summary(readings: List<Double>): String {
        // TODO: return "count:<n> avg:<avg>" using average() above.
        return ""
    }
}
