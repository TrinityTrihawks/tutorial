package org.first.tutorial

class Exercise {

    fun countCommands(commands: List<String>): Map<String, Int> {
        // TODO: build a frequency map.
        return emptyMap()
    }

    fun mostCommonCommand(counts: Map<String, Int>): String {
        // TODO: return the command with the highest count or "NONE" if empty.
        return ""
    }

    fun describeBinding(buttonBindings: Map<Int, String>, button: Int): String {
        // TODO: describe the binding or mark as unassigned.
        return ""
    }
}
