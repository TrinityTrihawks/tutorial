package org.first.tutorial

class Exercise {

    fun countObstacles(distancesCm: IntArray, dangerThresholdCm: Int): Int {
        // TODO: loop through distances and count how many are at or below the threshold.
        return 0
    }

    fun average(values: DoubleArray): Double {
        // TODO: return the mean; return 0 for an empty array.
        return 0.0
    }

    fun indexOfFirstBelow(distancesCm: IntArray, dangerThresholdCm: Int): Int {
        // TODO: return the first index where distance is at or below the threshold, otherwise -1.
        return -2
    }
}
