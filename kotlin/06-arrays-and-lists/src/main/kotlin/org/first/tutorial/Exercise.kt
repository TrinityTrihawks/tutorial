package org.first.tutorial

class Exercise {

    fun toWaypointList(waypoints: Array<String>): MutableList<String> {
        // TODO: return a mutable list copy of the array.
        return mutableListOf()
    }

    fun indexOfWaypoint(waypoints: List<String>, target: String): Int {
        // TODO: find the index of target or -1 if missing.
        return -2
    }

    fun joinCommands(commands: List<String>): String {
        // TODO: join commands with " -> " or return "EMPTY" if none.
        return ""
    }
}
